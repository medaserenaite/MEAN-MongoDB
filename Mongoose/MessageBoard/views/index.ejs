<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <title>Index</title>
</head>
<style>
    body{
        background-color: rgb(236, 250, 255);
    }
    h3{
        width: 500px;
        margin:auto;
        padding: 50px;
    }
    form{
       padding:20px;
       max-width: 500px;
       margin:auto;
       border: 1px solid darkblue;
       border-radius: 25px;
       background-color: white;
       
   }
   input{
       margin:10px;
   }
   #message{
       background-color: rgb(0, 0, 0);
       padding:25px;
       border-radius: 25px;
       border: 1px solid darkgray;
       box-shadow: 4px 4px 4px;
       max-width: 500px;
       margin: auto;
       color: white;
       font-family: dekko;
       letter-spacing: 3px;
   }
</style>
<body>
    <h3>The Dojo Message Board</h3>
    <form action="/addmessage" method="POST">
        <input type="text" class="form-control" placeholder="Name" name="name">
        <input type="text" class="form-control" placeholder="Message" name="messagetext">
        <input type="submit" class="btn btn-dark" name="submit">
    </form>
    <% for(m in mess){%>
        <div id="message">
        <h6>posted by:<%= mess[m].name %> </h6>
        <h3><%= mess[m].messagetext %></h3>       
        
        </div>
    <% } %>
    <% for(c in comm){%>
        <div id="comment">
            <h6>posted by:<%= comm[c].commenttext %></h6>
            <h5><%= comm[c].commenttext %></h5>
            <form action="/addcomment/<%= comm._id %>" method="POST">
                <input type="text" class="form-control" placeholder="Name" name="name">
                <input type="text" class="form-control" placeholder="Comment" name="commenttext">
                <input type="submit" class="btn btn-dark" name="submit">
            </form>     
        </div>
    <% } %>
</body>

<!-- <%- JSON.stringify(messages) %>  -->
<!-- based on the structure of your messages, determine how to display them -->
<% if(messages.addmessage) { %>
     <% for (var x of messages.addmessage) { %>
      <h3><%= x %></h3>
     <% } %>
 <% } %>
</html>
